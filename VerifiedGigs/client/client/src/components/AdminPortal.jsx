import { useEffect, useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import { useAuth } from "../context/useAuth";
import "./AdminPortal.css";

const API = "http://localhost:5000/api";
async function request(path, token, options = {}) { const response = await fetch(`${API}${path}`, { ...options, headers: { "Content-Type": "application/json", Authorization: `Bearer ${token}`, ...(options.headers || {}) } }); const body = await response.json().catch(() => ({})); if (!response.ok) throw new Error(body.message || "Request failed"); return body; }
const date = (value) => value ? new Date(value).toLocaleDateString("en-IN", { day: "numeric", month: "short", year: "numeric" }) : "Not specified";
const money = (value) => value === null || value === undefined || value === "" ? "Not specified" : `₹${Number(value).toLocaleString("en-IN")}`;
const errorText = (error) => error?.message || "Something went wrong. Please try again.";
function State({ loading, error, empty, children }) { if (loading) return <div className="admin-state"><span className="admin-spinner" />Loading...</div>; if (error) return <div className="admin-state error"><strong>Could not load this page</strong><span>{errorText(error)}</span></div>; if (empty) return <div className="admin-state"><strong>Nothing here yet</strong><span>{empty}</span></div>; return children; }
function Notice({ children, error = false }) { return children ? <div className={`admin-notice${error ? " error" : ""}`}>{children}</div> : null; }
function Shell({ title, children }) { const { user, logout } = useAuth(); const navigate = useNavigate(); const links = [["/admin/dashboard", "Dashboard"], ["/admin/users", "Users"], ["/admin/gigs", "Gigs"], ["/admin/verifications", "Verifications"], ["/admin/reports", "Reports"], ["/admin/catalog", "Categories & Skills"], ["/admin/profile", "Profile"]]; return <div className="admin-portal"><header className="admin-header"><Link className="admin-logo" to="/admin/dashboard"><span className="logo-mark">V</span><span>Verified<span>Gigs</span></span></Link><nav>{links.map(([to, label]) => <Link key={to} className={location.pathname === to ? "active" : ""} to={to}>{label}</Link>)}</nav><div className="admin-user"><span>{user?.name || user?.email || "Administrator"}</span><button onClick={() => { logout(); navigate("/"); }}>Log out</button></div></header><main className="admin-main"><div className="admin-heading"><div><p className="eyebrow"><span className="eyebrow-dot" />Platform Administration</p><h1>{title}</h1></div></div>{children}</main></div>; }



export function AdminGigs() { const { token } = useAuth(); const [gigs, setGigs] = useState([]); const [query, setQuery] = useState(""); const [status, setStatus] = useState("ALL"); const [loading, setLoading] = useState(true); const [error, setError] = useState(null); const [notice, setNotice] = useState(""); const load = () => { setLoading(true); request("/admin/gigs", token).then((body) => setGigs(body.gigs || [])).catch(setError).finally(() => setLoading(false)); }; useEffect(load, [token]); const remove = async (gigId) => { if (!window.confirm("Delete this gig? This cannot be undone.")) return; try { await request(`/admin/gigs/${gigId}`, token, { method: "DELETE" }); setNotice("Gig deleted successfully."); load(); } catch (err) { setError(err); } }; const visible = gigs.filter((gig) => (status === "ALL" || gig.status === status) && `${gig.title} ${gig.description} ${gig.company_name} ${gig.category_name}`.toLowerCase().includes(query.toLowerCase())); return <Shell title="Gigs"><div className="admin-toolbar"><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search all gigs" /><select value={status} onChange={(e) => setStatus(e.target.value)}><option value="ALL">All statuses</option><option>OPEN</option><option>IN_PROGRESS</option><option>COMPLETED</option><option>CANCELLED</option><option>CLOSED</option></select><span>{visible.length} gig{visible.length === 1 ? "" : "s"}</span></div>{notice && <Notice>{notice}</Notice>}<State loading={loading} error={error} empty={visible.length === 0 ? "No gigs match this filter." : null}><div className="admin-grid">{visible.map((gig) => <article className="admin-card" key={gig.gig_id}><div className="admin-kicker">{gig.status}<span>{gig.category_name}</span></div><h2>{gig.title}</h2><p>{gig.description}</p><div className="admin-meta"><span>{gig.company_name}</span><strong>{money(gig.budget_min)} - {money(gig.budget_max)}</strong></div><small>Deadline {date(gig.deadline)}</small><div className="admin-actions"><button className="admin-button danger" onClick={() => remove(gig.gig_id)}>Delete</button></div></article>)}</div></State></Shell>; }

export function AdminVerifications() { const { token } = useAuth(); const [documents, setDocuments] = useState([]); const [filter, setFilter] = useState("PENDING"); const [loading, setLoading] = useState(true); const [error, setError] = useState(null); const [notice, setNotice] = useState(""); const load = () => { setLoading(true); request("/verification-documents", token).then((body) => setDocuments(body.documents || [])).catch(setError).finally(() => setLoading(false)); }; useEffect(load, [token]); const update = async (id, status) => { try { const path = status === "VERIFIED" ? `/verification-documents/${id}/approve` : `/verification-documents/${id}/reject`; const body = status === "VERIFIED" ? {} : { rejectionReason: window.prompt("Enter rejection reason") || "No reason provided" }; await request(path, token, { method: "PUT", body: JSON.stringify(body) }); setNotice(`Document ${status.toLowerCase()}.`); load(); } catch (err) { setError(err); } }; const visible = documents.filter((item) => filter === "ALL" || item.verification_status === filter); return <Shell title="Verification queue"><div className="tabs">{["PENDING", "VERIFIED", "REJECTED", "ALL"].map((item) => <button className={filter === item ? "active" : ""} key={item} onClick={() => setFilter(item)}>{item[0] + item.slice(1).toLowerCase()}</button>)}</div>{notice && <Notice>{notice}</Notice>}<State loading={loading} error={error} empty={visible.length === 0 ? "No verification documents match this filter." : null}><div className="stack">{visible.map((item) => <article className="admin-card document-row" key={item.document_id}><div><div className="admin-kicker">{item.verification_status}<span>{date(item.uploaded_at)}</span></div><h2>{item.document_type}</h2><p>Student ID: {item.student_id}</p><a href={item.document_url} target="_blank" rel="noreferrer">Open document</a></div><div className="admin-actions">{item.verification_status === "PENDING" && <><button className="admin-button" onClick={() => update(item.document_id, "VERIFIED")}>Approve</button><button className="admin-button danger" onClick={() => update(item.document_id, "REJECTED")}>Reject</button></>}</div></article>)}</div></State></Shell>; }

export function AdminReports() { const { token } = useAuth(); const [reports, setReports] = useState([]); const [filter, setFilter] = useState("ALL"); const [loading, setLoading] = useState(true); const [error, setError] = useState(null); const [notice, setNotice] = useState(""); const load = () => { setLoading(true); request("/reports", token).then((body) => setReports(body.reports || [])).catch(setError).finally(() => setLoading(false)); }; useEffect(load, [token]); const update = async (id, status) => { try { await request(`/reports/${id}/status`, token, { method: "PUT", body: JSON.stringify({ status }) }); setNotice(`Report marked ${status.toLowerCase().replace("_", " ")}.`); load(); } catch (err) { setError(err); } }; const visible = filter === "ALL" ? reports : reports.filter((item) => item.report_status === filter); return <Shell title="Reports"><div className="tabs">{["ALL", "PENDING", "UNDER_REVIEW", "RESOLVED", "REJECTED"].map((item) => <button className={filter === item ? "active" : ""} key={item} onClick={() => setFilter(item)}>{item === "ALL" ? "All" : item.replace("_", " ")}</button>)}</div>{notice && <Notice>{notice}</Notice>}<State loading={loading} error={error} empty={visible.length === 0 ? "No reports match this filter." : null}><div className="stack">{visible.map((item) => <article className="admin-card report-row" key={item.report_id}><div><div className="admin-kicker">{item.report_status}<span>{date(item.reported_at)}</span></div><h2>{item.reason}</h2><p>{item.description || "No additional description."}</p><small>Reporter: {item.reporter_name || item.reporter_id} · Target: {item.reported_user_name || item.reported_user_id || `Gig ${item.gig_id || "not specified"}`}</small></div><div className="admin-actions"><select value={item.report_status} onChange={(e) => update(item.report_id, e.target.value)}><option>PENDING</option><option>UNDER_REVIEW</option><option>RESOLVED</option><option>REJECTED</option></select></div></article>)}</div></State></Shell>; }

export function AdminCatalog() { const { token } = useAuth(); const [categories, setCategories] = useState([]); const [skills, setSkills] = useState([]); const [form, setForm] = useState({ categoryName: "", categoryDescription: "", skillName: "", skillCategory: "", skillDescription: "" }); const [loading, setLoading] = useState(true); const [error, setError] = useState(null); const [notice, setNotice] = useState(""); const load = () => { setLoading(true); Promise.all([request("/categories", token), request("/skills", token)]).then(([categoryBody, skillBody]) => { setCategories(categoryBody.categories || []); setSkills(skillBody.skills || []); }).catch(setError).finally(() => setLoading(false)); }; useEffect(load, [token]); const create = async (type, event) => { event.preventDefault(); try { if (type === "category") await request("/categories", token, { method: "POST", body: JSON.stringify({ categoryName: form.categoryName, description: form.categoryDescription }) }); else await request("/skills", token, { method: "POST", body: JSON.stringify({ skillName: form.skillName, category: form.skillCategory, description: form.skillDescription }) }); setNotice(`${type[0].toUpperCase() + type.slice(1)} created successfully.`); load(); } catch (err) { setError(err); } }; return <Shell title="Categories and skills"><State loading={loading} error={error}>{notice && <Notice>{notice}</Notice>}{error && <Notice error>{errorText(error)}</Notice>}<div className="catalog-grid"><section className="admin-card"><h2>Categories</h2><div className="catalog-list">{categories.length ? categories.map((item) => <div key={item.category_id}><strong>{item.category_name}</strong><small>{item.description || "No description"}</small></div>) : <p className="muted">No categories found.</p>}</div><form onSubmit={(e) => create("category", e)}><label>Name<input required value={form.categoryName} onChange={(e) => setForm({ ...form, categoryName: e.target.value })} /></label><label>Description<textarea value={form.categoryDescription} onChange={(e) => setForm({ ...form, categoryDescription: e.target.value })} /></label><button className="admin-button">Add category</button></form></section><section className="admin-card"><h2>Skills</h2><div className="catalog-list">{skills.length ? skills.map((item) => <div key={item.skill_id}><strong>{item.skill_name}</strong><small>{item.category || "Uncategorized"}</small></div>) : <p className="muted">No skills found.</p>}</div><form onSubmit={(e) => create("skill", e)}><label>Name<input required value={form.skillName} onChange={(e) => setForm({ ...form, skillName: e.target.value })} /></label><label>Category<input value={form.skillCategory} onChange={(e) => setForm({ ...form, skillCategory: e.target.value })} /></label><label>Description<textarea value={form.skillDescription} onChange={(e) => setForm({ ...form, skillDescription: e.target.value })} /></label><button className="admin-button">Add skill</button></form></section></div><Notice>Existing APIs support listing and creation only. Edit and delete APIs are unavailable.</Notice></State></Shell>; }
